# MediaPipe 0.10.21 is the last line that still ships the legacy
# `mp.solutions.face_mesh` API used by src/lip_detector.py. It also pins
# numpy<2, so opencv is held to a numpy-1.x-compatible build to match.
av>=10.0.0
mediapipe==0.10.21
numpy<2
scipy>=1.11.0
opencv-python==4.11.0.86
